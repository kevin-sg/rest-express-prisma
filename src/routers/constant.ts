export const enum AuthPath {
	Login = '/auth/login',
	Logout = '/auth/logout',
	Revalidate = '/auth/revalidate',
}

export const enum UserPath {
	Base = '/user',
	ById = '/user/:id',
}

export const enum PostPath {
	Base = '/post',
	ById = '/post/:id',
}

export const enum SeedPath {
	Base = '/seed',
}
